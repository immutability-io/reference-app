{{define "content"}}
<div class="container">
    <div class="row">
        <div class="jumbotron">
            <h1>Hello, {{ .FirstName }} {{ .LastName }}!</h1>
            <p>Your entitlements are:</p>
            {{ range .Entitlements }}
            <p>{{ . }}</p>
            {{ end }}
        </div>
    </div>
</div>
{{end}}
