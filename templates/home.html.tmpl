{{define "content"}}
<div class="container">
    <div class="row">
        <div class="jumbotron">
            <h1>Hello, {{ .FirstName }} {{ .LastName }}!</h1>
            <p>Welcome to your new Go web project.</p>
        </div>
    </div>
</div>
{{end}}
